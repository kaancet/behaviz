# ColorbarSpec

!!! note "Stub — to be expanded"

Describes the colorbar attached to `plot_image`. Fields cover `label`, `location`,
`fraction`, `pad`, `ticks`, `tick_fmt`, `fontsize`.

_TODO: field table + example from README §"Colorbar — no plumbing required"._

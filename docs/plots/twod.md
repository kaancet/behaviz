# 2D & images

!!! note "Stub — to be expanded"

## Image / heatmap — `plot_image`

**Signature:** `plot_image(data, extent=None, origin="upper", cmap="viridis", vmin=None,
vmax=None, aspect="auto", ...)`.

`aspect="auto"` fills the axes (default); pass `aspect="equal"` for square pixels.

_TODO: example, extent/origin explanation._

## Colorbar — no plumbing required

A colorbar is described by a [`ColorbarSpec`](../specs/colorbar.md) and attached
automatically.

_TODO: example pulling from README §"Colorbar — no plumbing required"._

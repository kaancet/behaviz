# Distribution plots

!!! note "Stub — to be expanded"

## Violin — `plot_violin`

**Signature:** `plot_violin(x, ys, ...)` — `x` are positions, `ys` is a list of arrays (one
distribution per position).

_TODO: example, channels, backend notes._

## Hexbin — `plot_hexbin`

2D density via hexagonal binning. `gridsize=`, `cmap=`.

_TODO: example, colorbar note._

## Pie — `plot_pie`

_TODO: example, when not to use a pie chart._
